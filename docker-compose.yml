services:
  likebot:
    build: .
    container_name: likebot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      SESSION_DIR: /data
    volumes:
      - ./data:/data
      - ./bot.py:/app/bot.py:ro
    stdin_open: true
    tty: true
